import React from 'react'
import { InputCodeProps } from '~/@types/components'

const InputCode: React.FC<InputCodeProps> = ({ form, name, label }) => {
    return <div>InputCode</div>
}

export default InputCode
