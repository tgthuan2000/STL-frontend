import { motion } from 'framer-motion'

const SayHiImage = () => {
    return <motion.img layout layoutId='say-hi' src='/say-hi.png' className='h-full w-full object-cover' />
}

export default SayHiImage
